# Sources are BOM-less UTF-8 with LF line endings on every platform; text files
# are normalized to LF in the repository and checked out as LF everywhere.
* text=auto eol=lf

# Binary assets — never subject to text normalization.
*.png binary
*.jpg binary
*.ttf binary
*.otf binary
*.bin binary
